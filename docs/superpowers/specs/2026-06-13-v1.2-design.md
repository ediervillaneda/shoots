# v1.2 Design -- Starfall

**Fecha:** 2026-06-13
**Version:** v1.2
**Scope:** Menus, pausa, records locales. Refactor de Game para stack de escenas.
**Base:** v1.1 -- AudioSystem, SFX, musica en loop

---

## Estructura de archivos

`
src/game.py                    <- Modificar: stack de escenas push/pop
src/scenes/menu.py             <- Crear: MenuScene
src/scenes/game_over.py        <- Crear: GameOverScene con entrada de iniciales
src/systems/scores.py          <- Crear: carga/guardado JSON top 10
src/settings/scores.py         <- Crear: constantes scores
src/scenes/gameplay.py         <- Modificar: pausa con P/ESC, transicion a GameOverScene
tests/test_scores.py           <- Crear
tests/test_menu.py             <- Crear
`

---

## Arquitectura: stack de escenas

Game pasa de tener self.scene a gestionar un stack:

`python
class Game:
    def __init__(self):
        self._stack: list = []
        self.push_scene(MenuScene(self))

    def push_scene(self, scene) -> None:
        self._stack.append(scene)

    def pop_scene(self) -> None:
        self._stack.pop()
        if not self._stack:
            self._running = False

    def replace_scene(self, scene) -> None:
        self._stack[-1] = scene

    def run(self):
        while self._running and self._stack:
            scene = self._stack[-1]
            events = pygame.event.get()
            scene.process_input(events)
            scene.update(self._dt())
            scene.render(self._screen)
            pygame.display.flip()
            self._clock.tick(FPS)
`

Cada escena recibe game en su __init__ para poder llamar game.push_scene() / game.pop_scene() / game.replace_scene().

---

## MenuScene -- src/scenes/menu.py

Pantalla de titulo. Opciones: Jugar, Ver records, Salir.

Navegacion con flechas arriba/abajo o W/S. Seleccion con ENTER o SPACE.
Opcion seleccionada en amarillo, resto en gris.

Acciones:
- JUGAR -> game.push_scene(GameplayScene(game))
- RECORDS -> game.push_scene(ScoresScene(game))
- SALIR -> game.pop_scene()

---

## GameOverScene -- src/scenes/game_over.py

Muestra score final. Si entra en top 10, pide 3 iniciales con teclado.

`python
class GameOverScene:
    def __init__(self, game, score: int):
        self._game = game
        self._score = score
        self._scores = scores.load()
        self._is_highscore = scores.qualifies(score, self._scores)
        self._initials = ""
        self._saved = False
`

- Entrada de iniciales: hasta 3 caracteres alfa, BACKSPACE borra, ENTER confirma
- Despues de guardar o si no es highscore: SPACE/ENTER/ESC -> MenuScene

---

## ScoresScene

Tabla top 10 con posicion, iniciales y score. ESC/SPACE vuelve al menu.

---

## scores -- src/systems/scores.py

`python
import json
from pathlib import Path

_SCORES_PATH = Path.home() / ".starfall" / "scores.json"
_MAX_ENTRIES = 10


def load() -> list[dict]:
    if not _SCORES_PATH.exists():
        return []
    try:
        return json.loads(_SCORES_PATH.read_text())
    except (json.JSONDecodeError, OSError):
        return []


def save(initials: str, score: int, current: list[dict]) -> list[dict]:
    current.append({"initials": initials[:3].upper(), "score": score})
    current.sort(key=lambda x: x["score"], reverse=True)
    current = current[:_MAX_ENTRIES]
    _SCORES_PATH.parent.mkdir(parents=True, exist_ok=True)
    _SCORES_PATH.write_text(json.dumps(current, indent=2))
    return current


def qualifies(score: int, current: list[dict]) -> bool:
    if len(current) < _MAX_ENTRIES:
        return True
    return score > current[-1]["score"]
`

---

## GameplayScene -- cambios

- Recibe game en __init__
- Al llegar a game_over: game.replace_scene(GameOverScene(game, self.score))
- Pausa: estado "paused". P o ESC durante playing -> state = "paused"
- En pausa: ESC -> MenuScene; P/ENTER/SPACE -> reanudar

---

## Testing

### tests/test_scores.py
- load() retorna [] si el archivo no existe
- save() retorna lista ordenada descendente
- save() no excede MAX_ENTRIES
- qualifies() True si lista < 10; True si score > minimo; False si no
- Tolerancia a JSON corrupto -> []

### tests/test_menu.py
- MenuScene instancia sin crash
- Navegacion up/down cicla _selected correctamente
- ESC llama game.pop_scene()

---

## Criterio de completitud v1.2

- Juego abre en pantalla de titulo con 3 opciones navegables
- JUGAR lanza GameplayScene; SALIR cierra el juego
- Durante gameplay, P o ESC pausa; segundo ESC vuelve al menu
- Game over muestra score final
- Si es top 10: pide iniciales, guarda en ~/.starfall/scores.json
- RECORDS muestra top 10 con iniciales y scores
- Sin regresiones
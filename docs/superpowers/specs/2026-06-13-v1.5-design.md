# v1.5 Design -- Starfall

**Fecha:** 2026-06-13
**Version:** v1.5
**Scope:** Nuevos power-ups -- spread, plasma, laser
**Base:** v1.4 -- gun_upgrade divergente, shield halo, tinting completo

---

## Estructura de archivos

`
src/entities/laser.py         <- Crear: Laser(Sprite) rayo de area
src/entities/player.py        <- Modificar: shoot() maneja spread/plasma; shoot_laser()
src/entities/powerup.py       <- Modificar: agregar spread, plasma, laser
src/scenes/gameplay.py        <- Modificar: grupo lasers, colisiones laser-enemies
src/settings/player.py        <- Modificar: constantes spread, plasma, laser
tests/test_laser.py           <- Crear
tests/test_player.py          <- Modificar
`

---

## Constantes nuevas (src/settings/player.py)

`python
SPREAD_ANGLES     = [-30.0, -15.0, 0.0, 15.0, 30.0]
SPREAD_DAMAGE     = 1
PLASMA_SPEED_MULT = 0.5
PLASMA_DAMAGE     = 5
PLASMA_TINT       = (255, 140, 40, 255)
LASER_DURATION_MS = 3000
LASER_DAMAGE_PER_S = 10
LASER_W           = 8
LASER_COLOR       = (100, 220, 255)
`

---

## Laser -- src/entities/laser.py

`python
class Laser(pygame.sprite.Sprite):
    def __init__(self, player):
        super().__init__()
        self._player = player
        self._elapsed_ms = 0
        self.image = pygame.Surface((LASER_W, SCREEN_H), pygame.SRCALPHA)
        self.image.fill((*LASER_COLOR, 180))
        self.rect = self.image.get_rect(centerx=player.rect.centerx, bottom=player.rect.top)
        self.damage_per_s = LASER_DAMAGE_PER_S

    def update(self, dt: float) -> None:
        self._elapsed_ms += dt * 1000
        self.rect.centerx = self._player.rect.centerx
        self.rect.bottom = self._player.rect.top
        if self._elapsed_ms >= LASER_DURATION_MS:
            self.kill()
            self._player.active_powerups.discard("laser")
`

---

## Player.shoot() -- spread y plasma

spread: retornar 5 balas Shot2 con angulos de SPREAD_ANGLES, damage SPREAD_DAMAGE.

plasma: retornar 1 bala Shot6 con speed_mult=PLASMA_SPEED_MULT, damage=PLASMA_DAMAGE, tint=PLASMA_TINT.

Precedencia en shoot(): laser (tecla separada) > spread > plasma > gun_upgrade normal.

---

## GameplayScene -- cambios

- Nuevo grupo: self.lasers = pygame.sprite.Group()
- self._laser_active: Laser | None = None
- En process_input: si "laser" en powerups y Space presionado y no hay laser activo, crear Laser
- En update: lasers.update(dt); para cada laser activo, dano por dt a enemigos en colision

`python
if self._laser_active and self._laser_active.alive():
    for enemy in list(self.enemies):
        if pygame.sprite.collide_rect(self._laser_active, enemy):
            enemy.take_damage(LASER_DAMAGE_PER_S * dt)
            if not enemy.alive():
                # ... logica de muerte normal ...
`

---

## Testing

`python
def test_spread_dispara_5_balas():
    p = Player(); p.active_powerups.add("spread")
    assert len(p.shoot(0)) == 5

def test_plasma_dispara_1_bala_danio_alto():
    p = Player(); p.active_powerups.add("plasma")
    balas = p.shoot(0)
    assert len(balas) == 1
    assert balas[0].damage == PLASMA_DAMAGE

def test_laser_se_autodestruye_tras_duracion(pygame_init):
    p = Player(); p.active_powerups.add("laser")
    laser = Laser(p)
    laser.update(LASER_DURATION_MS / 1000 + 0.1)
    assert not laser.alive()
`

---

## Criterio de completitud v1.5

- spread: 5 balas en abanico +/-30, cubren pantalla horizontalmente
- plasma: 1 bala enorme Shot6, lenta, damage 5, explosion grande
- laser: rayo 3s desde nave hasta borde, dana todos en columna
- Sin power-ups nuevos: disparo normal sin cambios
- Grupo lasers limpiado en _reset()
- Tests pasando, 0 regresiones
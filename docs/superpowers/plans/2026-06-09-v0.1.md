# v0.1 Implementation Plan — Starfall

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ventana Pygame 480×640, nave jugadora con movimiento WASD en 8 direcciones, disparo con Espacio — todo como rectángulos de color (placeholders).

**Architecture:** `Game` posee el loop principal y una scene activa (`GameplayScene`). `GameplayScene` posee `Player` y un `Group` de bullets, delegando input/update/render cada frame. Movimiento frame-rate independent con `dt` (segundos).

**Tech Stack:** Python 3.13+, Pygame Community Edition (`pygame-ce`), pytest

---

## Archivos a crear

| Archivo | Responsabilidad |
|---------|----------------|
| `src/__init__.py` | Marca `src/` como package |
| `src/settings.py` | Todas las constantes del juego |
| `src/scenes/__init__.py` | Marca `src/scenes/` como package |
| `src/scenes/gameplay.py` | `GameplayScene`: orquesta input/update/render |
| `src/entities/__init__.py` | Marca `src/entities/` como package |
| `src/entities/player.py` | `Player(Sprite)`: movimiento, disparo |
| `src/entities/bullet.py` | `Bullet(Sprite)`: movimiento hacia arriba, auto-kill |
| `src/game.py` | `Game`: inicializa pygame, loop principal |
| `main.py` | Entry point |
| `tests/__init__.py` | Marca `tests/` como package |
| `tests/conftest.py` | Fixture pygame headless para todos los tests |
| `tests/test_bullet.py` | Tests unitarios de Bullet |
| `tests/test_player.py` | Tests unitarios de Player |

---

### Task 1: Scaffolding + settings.py

**Files:**
- Create: `src/__init__.py`
- Create: `src/scenes/__init__.py`
- Create: `src/entities/__init__.py`
- Create: `tests/__init__.py`
- Create: `src/settings.py`
- Create: `assets/` (directorio vacío con `.gitkeep`)

- [ ] **Step 1: Instalar dependencias**

```powershell
pip install pygame-ce pytest
```

Expected: instalación exitosa sin errores.

- [ ] **Step 2: Crear directorios y `__init__.py` vacíos**

```powershell
New-Item -ItemType Directory -Path "F:\apps\python\shoots\src\scenes" -Force
New-Item -ItemType Directory -Path "F:\apps\python\shoots\src\entities" -Force
New-Item -ItemType Directory -Path "F:\apps\python\shoots\tests" -Force
New-Item -ItemType Directory -Path "F:\apps\python\shoots\assets" -Force
New-Item -ItemType File -Path "F:\apps\python\shoots\src\__init__.py" -Force
New-Item -ItemType File -Path "F:\apps\python\shoots\src\scenes\__init__.py" -Force
New-Item -ItemType File -Path "F:\apps\python\shoots\src\entities\__init__.py" -Force
New-Item -ItemType File -Path "F:\apps\python\shoots\tests\__init__.py" -Force
New-Item -ItemType File -Path "F:\apps\python\shoots\assets\.gitkeep" -Force
```

- [ ] **Step 3: Crear `src/settings.py`**

```python
SCREEN_W = 480
SCREEN_H = 640
FPS = 60
TITLE = "Starfall"

# Player
PLAYER_SPEED = 300      # px/s (≈5 px/frame a 60fps)
PLAYER_COLOR = (100, 180, 255)
PLAYER_W = 40
PLAYER_H = 50

# Bullet
BULLET_SPEED = 720      # px/s (≈12 px/frame a 60fps)
BULLET_DAMAGE = 1
BULLET_COLOR = (255, 255, 100)
BULLET_W = 6
BULLET_H = 14
BULLET_COOLDOWN = 250   # ms entre disparos
```

- [ ] **Step 4: Verificar imports**

```powershell
cd F:\apps\python\shoots
python -c "from src.settings import SCREEN_W, FPS, PLAYER_SPEED; print('OK')"
```

Expected: `OK`

- [ ] **Step 5: Commit**

```powershell
git -C "F:\apps\python\shoots" add src/__init__.py src/scenes/__init__.py src/entities/__init__.py src/settings.py tests/__init__.py assets/.gitkeep
git -C "F:\apps\python\shoots" commit -m "feat: scaffold project structure and settings"
```

---

### Task 2: Bullet entity

**Files:**
- Create: `src/entities/bullet.py`
- Create: `tests/conftest.py`
- Create: `tests/test_bullet.py`

- [ ] **Step 1: Crear `tests/conftest.py`**

```python
import os
import pytest
import pygame

@pytest.fixture(autouse=True, scope="session")
def pygame_init():
    os.environ.setdefault("SDL_VIDEODRIVER", "dummy")
    os.environ.setdefault("SDL_AUDIODRIVER", "dummy")
    pygame.init()
    pygame.display.set_mode((1, 1))
    yield
    pygame.quit()
```

- [ ] **Step 2: Escribir tests que fallan**

```python
# tests/test_bullet.py
import pygame
from src.entities.bullet import Bullet
from src.settings import BULLET_SPEED, BULLET_H

def test_bullet_moves_up():
    bullet = Bullet(240, 400)
    initial_y = bullet.y
    bullet.update(1.0)
    assert bullet.y == initial_y - BULLET_SPEED

def test_bullet_stays_alive_mid_screen():
    bullet = Bullet(240, 400)
    group = pygame.sprite.Group(bullet)
    bullet.update(0.016)
    assert bullet.alive()

def test_bullet_killed_when_off_screen():
    bullet = Bullet(240, BULLET_H)  # rect.top = 0, barely on screen
    group = pygame.sprite.Group(bullet)
    bullet.update(1.0)              # sube 720px — fuera de pantalla
    assert not bullet.alive()

def test_bullet_rect_x_matches_spawn():
    bullet = Bullet(100, 300)
    assert bullet.rect.centerx == 100
```

- [ ] **Step 3: Correr tests — deben fallar**

```powershell
cd F:\apps\python\shoots
pytest tests/test_bullet.py -v
```

Expected: `ImportError` o `ModuleNotFoundError` (Bullet no existe aún).

- [ ] **Step 4: Crear `src/entities/bullet.py`**

```python
import pygame
from src.settings import BULLET_SPEED, BULLET_COLOR, BULLET_W, BULLET_H


class Bullet(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.Surface((BULLET_W, BULLET_H))
        self.image.fill(BULLET_COLOR)
        self.rect = self.image.get_rect(centerx=x, bottom=y)
        self.y = float(self.rect.y)

    def update(self, dt):
        self.y -= BULLET_SPEED * dt
        self.rect.y = int(self.y)
        if self.rect.bottom < 0:
            self.kill()
```

- [ ] **Step 5: Correr tests — deben pasar**

```powershell
pytest tests/test_bullet.py -v
```

Expected: `4 passed`

- [ ] **Step 6: Commit**

```powershell
git -C "F:\apps\python\shoots" add src/entities/bullet.py tests/conftest.py tests/test_bullet.py
git -C "F:\apps\python\shoots" commit -m "feat: add Bullet entity"
```

---

### Task 3: Player entity

**Files:**
- Create: `src/entities/player.py`
- Create: `tests/test_player.py`

- [ ] **Step 1: Escribir tests que fallan**

```python
# tests/test_player.py
import math
import pygame
from src.entities.player import Player, calc_velocity
from src.settings import SCREEN_W, SCREEN_H, PLAYER_W, PLAYER_H, BULLET_COOLDOWN

# --- calc_velocity (función pura, no necesita Sprite) ---

def test_velocity_right():
    vx, vy = calc_velocity(left=False, right=True, up=False, down=False)
    assert vx == 1.0 and vy == 0.0

def test_velocity_left():
    vx, vy = calc_velocity(left=True, right=False, up=False, down=False)
    assert vx == -1.0 and vy == 0.0

def test_velocity_up():
    vx, vy = calc_velocity(left=False, right=False, up=True, down=False)
    assert vx == 0.0 and vy == -1.0

def test_velocity_diagonal_normalized():
    vx, vy = calc_velocity(left=False, right=True, up=True, down=False)
    magnitude = math.sqrt(vx**2 + vy**2)
    assert abs(magnitude - 1.0) < 0.001

def test_velocity_idle():
    vx, vy = calc_velocity(left=False, right=False, up=False, down=False)
    assert vx == 0.0 and vy == 0.0

# --- Player sprite ---

def test_player_initial_position():
    player = Player()
    assert player.rect.centerx == SCREEN_W // 2
    assert player.rect.bottom == SCREEN_H - 80

def test_player_clamped_to_screen_right():
    player = Player()
    player.rect.x = SCREEN_W + 100  # fuera de pantalla
    player.x = float(player.rect.x)
    player.vel_x = 0.0
    player.vel_y = 0.0
    player.update(0.016)
    assert player.rect.right <= SCREEN_W

def test_player_clamped_to_screen_left():
    player = Player()
    player.rect.x = -200
    player.x = float(player.rect.x)
    player.vel_x = 0.0
    player.vel_y = 0.0
    player.update(0.016)
    assert player.rect.left >= 0

def test_player_shoot_returns_bullet():
    from src.entities.bullet import Bullet
    player = Player()
    player.last_shot = 0
    bullet = player.shoot(BULLET_COOLDOWN + 1)
    assert bullet is not None
    assert isinstance(bullet, Bullet)

def test_player_shoot_respects_cooldown():
    player = Player()
    player.last_shot = 1000
    bullet = player.shoot(1000 + BULLET_COOLDOWN - 1)
    assert bullet is None

def test_player_bullet_spawns_at_player_top():
    from src.entities.bullet import Bullet
    player = Player()
    player.last_shot = 0
    bullet = player.shoot(BULLET_COOLDOWN + 1)
    assert bullet.rect.centerx == player.rect.centerx
    assert bullet.rect.bottom == player.rect.top
```

- [ ] **Step 2: Correr tests — deben fallar**

```powershell
cd F:\apps\python\shoots
pytest tests/test_player.py -v
```

Expected: `ImportError` (`Player` no existe).

- [ ] **Step 3: Crear `src/entities/player.py`**

```python
import pygame
from src.settings import (
    PLAYER_SPEED, PLAYER_COLOR, PLAYER_W, PLAYER_H,
    SCREEN_W, SCREEN_H, BULLET_COOLDOWN,
)
from src.entities.bullet import Bullet

SQRT2_INV = 0.7071067811865476  # 1/sqrt(2)


def calc_velocity(left, right, up, down):
    """Calcula vector de velocidad normalizado a partir de teclas booleanas."""
    dx = float(right) - float(left)
    dy = float(down) - float(up)
    if dx != 0 and dy != 0:
        dx *= SQRT2_INV
        dy *= SQRT2_INV
    return dx, dy


class Player(pygame.sprite.Sprite):
    def __init__(self):
        super().__init__()
        self.image = pygame.Surface((PLAYER_W, PLAYER_H))
        self.image.fill(PLAYER_COLOR)
        self.rect = self.image.get_rect(
            centerx=SCREEN_W // 2,
            bottom=SCREEN_H - 80,
        )
        self.x = float(self.rect.x)
        self.y = float(self.rect.y)
        self.vel_x = 0.0
        self.vel_y = 0.0
        self.last_shot = 0

    def handle_keys(self, keys):
        self.vel_x, self.vel_y = calc_velocity(
            left=bool(keys[pygame.K_a]),
            right=bool(keys[pygame.K_d]),
            up=bool(keys[pygame.K_w]),
            down=bool(keys[pygame.K_s]),
        )

    def update(self, dt):
        self.x += self.vel_x * PLAYER_SPEED * dt
        self.y += self.vel_y * PLAYER_SPEED * dt
        self.x = max(0.0, min(float(SCREEN_W - PLAYER_W), self.x))
        self.y = max(0.0, min(float(SCREEN_H - PLAYER_H), self.y))
        self.rect.x = int(self.x)
        self.rect.y = int(self.y)

    def shoot(self, now):
        if now - self.last_shot >= BULLET_COOLDOWN:
            self.last_shot = now
            return Bullet(self.rect.centerx, self.rect.top)
        return None
```

- [ ] **Step 4: Correr tests — deben pasar**

```powershell
pytest tests/test_player.py -v
```

Expected: `11 passed`

- [ ] **Step 5: Correr suite completa**

```powershell
pytest -v
```

Expected: `15 passed` (4 bullet + 11 player)

- [ ] **Step 6: Commit**

```powershell
git -C "F:\apps\python\shoots" add src/entities/player.py tests/test_player.py
git -C "F:\apps\python\shoots" commit -m "feat: add Player entity with movement and shooting"
```

---

### Task 4: GameplayScene

**Files:**
- Create: `src/scenes/gameplay.py`

- [ ] **Step 1: Crear `src/scenes/gameplay.py`**

```python
import pygame
from src.entities.player import Player


class GameplayScene:
    def __init__(self):
        self.player = Player()
        self.bullets = pygame.sprite.Group()
        self.all_sprites = pygame.sprite.Group(self.player)

    def process_input(self, events):
        keys = pygame.key.get_pressed()
        self.player.handle_keys(keys)
        if keys[pygame.K_SPACE]:
            bullet = self.player.shoot(pygame.time.get_ticks())
            if bullet:
                self.bullets.add(bullet)
                self.all_sprites.add(bullet)

    def update(self, dt):
        self.player.update(dt)
        self.bullets.update(dt)

    def render(self, screen):
        screen.fill((0, 0, 0))
        self.all_sprites.draw(screen)
```

- [ ] **Step 2: Verificar import**

```powershell
cd F:\apps\python\shoots
python -c "from src.scenes.gameplay import GameplayScene; print('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```powershell
git -C "F:\apps\python\shoots" add src/scenes/gameplay.py
git -C "F:\apps\python\shoots" commit -m "feat: add GameplayScene"
```

---

### Task 5: Game class + main.py

**Files:**
- Create: `src/game.py`
- Create: `main.py`

- [ ] **Step 1: Crear `src/game.py`**

```python
import pygame
from src.settings import SCREEN_W, SCREEN_H, FPS, TITLE
from src.scenes.gameplay import GameplayScene


class Game:
    def __init__(self):
        pygame.init()
        self.screen = pygame.display.set_mode((SCREEN_W, SCREEN_H))
        pygame.display.set_caption(TITLE)
        self.clock = pygame.time.Clock()
        self.scene = GameplayScene()

    def run(self):
        running = True
        while running:
            dt = self.clock.tick(FPS) / 1000.0
            events = pygame.event.get()
            for event in events:
                if event.type == pygame.QUIT:
                    running = False
                if event.type == pygame.KEYDOWN and event.key == pygame.K_ESCAPE:
                    running = False
            self.scene.process_input(events)
            self.scene.update(dt)
            self.scene.render(self.screen)
            pygame.display.flip()
        pygame.quit()
```

- [ ] **Step 2: Crear `main.py`**

```python
from src.game import Game

if __name__ == "__main__":
    Game().run()
```

- [ ] **Step 3: Verificar import**

```powershell
cd F:\apps\python\shoots
python -c "from src.game import Game; print('OK')"
```

Expected: `OK`

- [ ] **Step 4: Smoke test manual**

```powershell
cd F:\apps\python\shoots
python main.py
```

Expected:
- Ventana 480×640 se abre con título "Starfall"
- Fondo negro
- Rectángulo azul claro (nave) en centro-inferior
- WASD mueve la nave en 8 direcciones
- Nave no sale de la pantalla
- Espacio dispara rectángulos amarillos que suben
- Cooldown funciona (no spam infinito)
- ESC cierra la ventana

- [ ] **Step 5: Correr suite completa de tests**

```powershell
cd F:\apps\python\shoots
pytest -v
```

Expected: `15 passed`

- [ ] **Step 6: Commit final**

```powershell
git -C "F:\apps\python\shoots" add src/game.py main.py
git -C "F:\apps\python\shoots" commit -m "feat: add Game loop and main entry point"
```
